# HANDOVER — Webflow→Astro-Migration tristanweithaler.com

**Stand: 2026-07-21, Session 2 (Fable): Schritte 1-9 + 11 der Restliste sind ERLEDIGT (Build/Checks grün, committed — siehe README.md für den finalen Stand). OFFEN: Schritt 10 Provisioning/Deploy (`npm run provision -- … --execute` wurde von der Berechtigungs-Freigabe blockiert; braucht Julians Ausführung) + die im README gelisteten Platzhalter (Form.Taxi-Endpunkt, Vimeo-Poster, Datenschutztext, DNS). Der Rest dieses Dokuments ist die historische Übergabe aus Session 1.**

---

**Stand: 2026-07-20, Session 1 (Fable). Status: MITTEN IM UMBAU — Build aktuell ROT (bewusst, siehe „Roter Zustand"). Dieses Dokument ist die vollständige Übergabe an die nächste Session.**

## Auftrag (Julian, Original-Anforderungen)

1:1-Migration der Live-Site https://tristanweithaler.com (Webflow) auf dieses Astro+Sanity-Starter-Template. Optisch/funktional identisch (Typo, Farben, Abstände, Responsive, Animationen). Webflow-Code NUR als Referenz — sauberer Astro-Code, keine Webflow-Klassen. Sanity von Anfang an (Section-Modell des Templates), Presentation/Click-to-edit/Live-Preview MÜSSEN wie im Template funktionieren. Animationen einheitlich mit GSAP. Formulare → Form.Taxi-PLATZHALTER. End-to-End: bauen, GitHub pushen, Cloudflare deployen, technischer Abschlusscheck, separates Review durch Review-Agent, Fixes. Julian macht das visuelle Review selbst.

**Julians Antworten auf die Startfragen:**
1. **Cloudflare: Tristans Konto — ✅ GELÖST (2026-07-21).** Julian hat die Einladung angenommen und wrangler neu eingeloggt; `wrangler whoami` zeigt jetzt „Tristanweithaler@gmail.com's Account" = **ee216d2fdff2e29b5093ae6287b5eb8f**. Diese account_id ist in `wrangler.jsonc` + `wrangler.prod.jsonc` FEST GEPINNT — Deploys landen damit automatisch in Tristans Konto. Vor dem Provisioning trotzdem einmal `npx wrangler whoami` gegenprüfen. (Julians eigenes Konto b6a131… NICHT verwenden.)
2. **Seitenumfang:** NUR die Live-Seiten: `/` (Home), `/ueber-mich`, `/all-in-one-coaching`, `/datenschutz`, `/impressum`, 404, + neue `/danke` (noindex, Form.Taxi-Fallback). NICHT: work, resources, projects/apple, detail_service/testimonial (auf Live-Site 404/leer, Template-Leftovers).
3. **Cookie-Banner:** 1:1 nachbauen (eigene schlanke Implementierung statt Finsweet), **Vimeo-Embed consent-gated** (vorher Poster + „Video laden"-Button).

## Referenzmaterial

- **Specs (VOLLSTÄNDIG, aus Webflow-Export dekodiert):** `docs/webflow-spec/*.md` — global-chrome.md (Design-System, Header/Footer/Modals/Cookie-Banner inkl. exakter Werte), home.md, ueber-mich.md, all-in-one-coaching.md (Section-für-Section-Specs mit allen Texten wörtlich), animations.md (IX2 dekodiert, GSAP-Mapping), legal-layout.md, cms-data.md, content-datenschutz.md/content-impressum.md.
- **Webflow-Export:** `/Users/julian/Downloads/upgreight-Websites/Astro/tristan/tristan-webflow-code/` (Referenz bei Detailfragen).
- **Template-Doku:** README.md (Pflicht!), docs/sanity-live-editing-architecture.md, .claude/skills/upgreight-astro-website (Skill mit references/sanity.md).

## Zentrale Design-Entscheidungen (getroffen, gilt)

- **Einsprachig DE** (Live-Site rein deutsch) — Reduktion nach README-Anleitung ist ERLEDIGT und committed (Commit „Einsprachig (DE)…"). Singletons: `homePage`, `siteSettings` (feste IDs ohne Sprachsuffix).
- **Dunkles Token-Theme:** `--paper`=#0C0C0C, `--ink`=#F5F5F5, Gold `--brand`=#806429, Creme `--brand-cream`=#D8D3CC. Tone-Mapping: light=dunkler Default, alt=#1D1D1D, dark=INVERS hell, brand=Gold. Studio-Labels dafür noch anpassen (shared.ts toneField: „Standard (dunkel)"/„Dunkelgrau"/„Hell (invertiert)"/„Gold").
- **Responsive:** Breakpoint-Sprünge 991/767/479 via Media-Query-Overrides der Größen-Tokens (tokens.css), KEINE clamps — 1:1 wie Webflow.
- **18 neue Section-Typen** (dedizierte, strukturierte Felder — Interfaces KOMPLETT definiert in `src/lib/content/types.ts`, Projektion+Mapper KOMPLETT in `src/lib/content/sections.ts`): sectionHomeHero, sectionValueStatement, sectionResults, sectionSplitCta (layout glow|plain), sectionServicesTabs, sectionGalleryMarquee (Bekannt-aus UND AIO-Säulen, titlesVisible-Flag), sectionUspList, sectionTestimonials, sectionFaq, sectionVideoHero, sectionModule (Modul + „Deine Resultate", optionaler 1:1-Coaching-Teil mit Video), sectionBonuses, sectionFinalCta, sectionPortraitHero, sectionTimeline, sectionInterests, sectionPageHeader, sectionRichText. Plus Dokument-Typen `service` + `testimonial`.
- **Services/Testimonials werden per GROQ-Subquery IN die Section-Daten eingebettet** (siehe SERVICES_SUB/TESTIMONIALS_SUB in sections.ts) → funktioniert in Build UND Live-Island (groq-js löst Subqueries + asset-> lokal auf). Kein Click-to-edit auf einzelnen Service-Karten (bewusst, V1) — Pflege über Studio-Structure.
- **Bilder:** `mapImage()` in sections.ts ist browser-sicher und akzeptiert SEED-SiteImages (kind local) UND Sanity-Assets. React-Sections rendern via `Img.tsx` (plain <img>, Assets sind bereits avif/webp).
- **Seed-Strategie (WICHTIG):** EINE Quelle `shared/site-content.mjs` (NOCH ZU SCHREIBEN) mit `buildContent({ img })`-Builder. Konsumenten: `src/lib/content/seed.ts` (img → lokale Assets via images.ts-Registry) und `studio/scripts/make-seed.mjs` (img → `{_type:'image', _sanityAsset:'image@file://<absoluter Pfad>', alt}` — sanity dataset import lädt die Dateien dann hoch). Rechtstexte: FERTIG als `shared/legal/datenschutz-pt.mjs` (218 Blöcke) + `impressum-pt.mjs` (9 Blöcke), generiert von `scripts/build-legal-pt.mjs` aus shared/legal/*.md. CMS-Daten: FERTIG als `shared/data/services.mjs` (16 aktive) + `testimonials.mjs` (32), Bild-Pfade relativ zu src/assets/images (z. B. "services/xyz.avif"). Die 3 Draft-Services wurden NICHT übernommen (Live-Parität).
- **Kein Lenis, keine Seiten-Transition, kein Typekit, kein jQuery** — auf der Live-Site tot/inaktiv (animations.md §Seiten-Transition + §Nicht aktive Technik). smooth-scroll.ts wird NICHT initialisiert (aus motion.ts entfernen!); Lenis-Dependency kann aus package.json raus.
- **Marquee der Modul-Banner ist STATISCH** (kein Loop — so ist es live). Interests-Wort-Marquee ist CSS-Animation 22s (fertig in sections.css).
- **Videos:** nur die `_mp4.mp4`-Varianten übernommen → `public/videos/modul1-5.mp4` + `modulN-poster.jpg` (~17 MB). webm bewusst weggelassen (Browser nehmen eh die erste mp4-Source).
- **Vimeo** (AIO-Hero, ID 1164742630): consent-gated auf Kategorie-Zustimmung; Poster-Fallback = `posterImage` (Seed: og-aio.avif = Portrait) — Julian ggf. echtes Standbild geben lassen.
- **SEO-Titles:** Original-Titles 1:1 in seo.title; SEO.astro-Suffixlogik MUSS angepasst werden: kein „| Marke"-Suffix, wenn title bereits siteName enthält (sonst „Tristan Weithaler - Über mich | Tristan Weithaler").
- **Footer:** Live-Links „Work"(404!) und „Partner"(#) NICHT übernehmen. „Created by upgreight"-Credit (Hausregel) ergänzen. Lottie-Social-Icons: `public/lottie/instagram.json`+`linkedin.json`, via lottie-web light lazy (Autoplay 1×, Hover-Replay; G3 in animations.md).
- **Legal-Seiten:** noindex (wie Original + Hausregel), aus Sitemap raus. Datenschutz-Text erwähnt noch Webflow/Google/etc. — 1:1 übernommen, Julian im Abschluss-Summary darauf hinweisen (Rechtstext muss er anpassen lassen).
- **404:** gebrandet neu (Live-404 ist ungebrandetes Webflow-Default) — bewusste Abweichung, im Summary nennen.
- **Slug/Namen:** Projekt-Slug `tristanweithaler`, Worker `tristanweithaler-prod`/`-preview`, Studio-Host-Vorschlag `tristanweithaler`. Git-Remote: https://github.com/trischi1993/tw-website.git (leer, WRITE-Zugriff via gh als „upgreight" ok).

## Was FERTIG ist (committed bzw. in diesem WIP-Commit)

1. **Init:** init.mjs gelaufen (Platzhalter ersetzt, frische Git-Historie), .env auf Seed-Modus (dormant), Remote gesetzt.
2. **Einsprachig DE komplett** (App+Studio+Seed+Configs) — verifiziert grün (astro check 0/0, studio tsc 0, Prod-Build React-frei) im Commit 08d0e20.
3. **Assets komplett:** `public/fonts/poppins-*.woff2` (300–800+Italic), `public/videos/`, `public/lottie/`, `public/favicon.png`+`apple-touch-icon.png`, `public/og-default.png` (1200×630), `src/assets/images/` (42 Design-Bilder, normalisierte Namen — Zuordnung Alt→Neu siehe Git-Diff dieses Commits) + `services/` (16) + `testimonials/` (32) vom Webflow-CDN geladen, `og-home.png`/`og-about.jpg`/`og-aio.avif`.
4. **Styles komplett:** fonts.css, tokens.css (Webflow-Werte+Breakpoints), global.css (Buttons: .btn-glow mit Original-Gradient --gradient-btn, .button outline, .link-underline; Kontextklassen; Prose), sections.css (ALLE 18 Sections, Werte aus Specs).
5. **Content-Kontrakt komplett:** types.ts (alle Interfaces), sections.ts (Projektion inkl. Subqueries + mapImage + alle Mapper-Fälle), shared/editor-blocks.ts (alle Sections registriert).
6. **React-Komponenten komplett:** alle 18 Section-Komponenten + Img.tsx, ProseText.tsx, GlowButton.tsx, RichText.tsx (paragraphs-Modus) + SectionsList.tsx mit allen Fällen. data-anim/data-*-Hooks für Animationen und Widgets sind gesetzt (Konvention siehe unten).
7. **Legal-PT + CMS-Datenmodule** (siehe Seed-Strategie).

## ⚠️ ROTER ZUSTAND — was aktuell kaputt ist (bekannt, gewollt gestoppt)

1. `src/preview/insertables.ts`: `INSERT_FACTORIES` ist `Record<EditorBlockType,…>` → durch die neuen Registry-Einträge fehlen Keys → **astro check FEHLER**. Fix: Factories für alle neuen Sections ergänzen (sinnvolle sichtbare Defaults) ODER Typ auf Partial + null-Fallback ändern.
2. **Studio kennt die neuen Section-Typen nicht:** `sectionsField` speist `of[]` aus SECTION_TYPES (shared/editor-blocks) → `sanity schema validate`/dev ROT, bis alle Schemas existieren und in `studio/schemas/index.ts` registriert sind.
3. `src/lib/content/seed.ts` ist noch der generische Platzhalter (baut aber). `shared/site-content.mjs` existiert noch nicht.
4. Chrome (Header/Footer/Modals/Cookie), Scripts (motion/widgets/modals/consent), Seiten sind noch Template-Stand.
5. Ungeprüft: neue .tsx kompilieren noch nicht verifiziert (svg-Import in GlowButton: `typeof arrowIcon === 'string'`-Guard vorhanden).

## NÄCHSTE SCHRITTE (Reihenfolge für die neue Session)

1. **Studio-Schemas** für 18 Sections + `service`/`testimonial`-Dokumente schreiben (Feldnamen EXAKT == types.ts == Projektion; Muster: studio/schemas/objects/sections/sectionText.ts; geteilte Felder aus shared.ts nutzen — bei Layout-Sections (Heroes, Timeline, Results, Module) nur nameField+anchorField, KEINE sectionStyleFields, die verwalten ihr Layout selbst; Content-Sections (SplitCta, UspList, Faq, Bonuses, FinalCta, Testimonials, ServicesTabs, Gallery, Interests, PageHeader, RichText, ValueStatement) bekommen toneField+sectionStyleFields mit passenden initialValues (meist paddingTop/Bottom 'large'). `service`: name, formName, category (radio personal|business mit Original-Labels), description (text), image (imageWithAlt), order (number). `testimonial`: name, role, text (text), image, order. Structure: zwei documentTypeListItems „Coachings" + „Testimonials" (order asc). imageWithAlt-Feldnamen beachten: Section-Bildfelder sind type 'imageWithAlt'. `sectionRichText.body`: eigener PT-Array-Typ mit styles normal/h2/h3/h4 + bullet-Liste + Link-Annotation + strong/em (NICHT das constrained richTextField). toneField-Labels umbenennen (siehe Design-Entscheidungen).
2. **insertables.ts** ergänzen (Punkt 1 des roten Zustands).
3. **`shared/site-content.mjs`** schreiben (+ `shared/site-content.d.ts` für TS): buildContent({img}) → { siteSettings, home, pages[ueber-mich, all-in-one-coaching, datenschutz, impressum], services, testimonials }. ALLE Texte wörtlich aus docs/webflow-spec/home.md, ueber-mich.md, all-in-one-coaching.md (dort komplett enthalten: FAQs, USPs, Module-Bullets, Timeline-Beschreibungen …). Kontaktdaten aus content-impressum.md (Katharinaberg 50, 39020 Schnals, Italien; Tel +39 334 179 9393; info@tristanweithaler.com; MwSt IT03102180217). Exakte 1:1-Coaching-Texte der Module (aus Export extrahiert):
   - M1: „Nach diesem Modul folgt ein zweistündiger 1:1 Videocall, in dem wir offene Fragen klären, deine Positionierung reflektieren und ein stabiles Fundament schaffen."
   - M2: „…, deine Strategie schärfen und ich gezieltes Feedback für die Umsetzung gebe." (gleicher Satzanfang wie M1: „Nach diesem Modul folgt ein zweistündiger 1:1 Videocall, in dem wir offene Fragen klären, …")
   - M3: „…, deine Ergebnisse analysieren und deine Strategie gezielt optimieren."
   - M4: „Nach diesem Modul folgt ein zweistündiger 1:1 Videocall, in dem wir offene Fragen klären und dein Angebot sowie die Monetarisierung gezielt feinjustiert werden."
   - M5 (abweichend): „Dieses Modul ist ein praktisches 1:1 Coaching vor Ort (ca. 4 Stunden), in dem ich dir zeige, wie du hochwertigen Content einfach mit dem Handy produzierst und bearbeitest."
   Seiten-SEO: Original-Titles/Descriptions aus den Spec-Metas; Legal: noindex true. siteSettings um `headerCta {label:'0 € Angebot', href:'/#0-Euro-Angebot'}` erweitern (Typ+Schema+Query+Mapper mitziehen!).
4. **seed.ts + images.ts + make-seed.mjs** auf den Builder umstellen. images.ts: import.meta.glob-Registry über src/assets/images/**. make-seed: _sanityAsset-file://-Pfade absolut; `studio/package.json` „import-seed" um vorgelagertes `npm run seed:build` ergänzen. seed.ndjson regenerieren.
5. **Chrome:** Header.astro (fixe Navbar blur, Logo „Tristan|Weithaler"/„T|W" Goldlinie, Links aus settings.nav — externe mit target _blank, Glow-CTA aus settings.headerCta, Burger ≤991 + Fullscreen-Menü #121212 mit großen Links + Pfeil `icon-arrow-white.svg` + Hover-Wipes; Menü-Einträge = Startseite + nav) · Footer.astro (Dolomiten `footer-dolomiten.avif` auf #1D1D1D, Logo `logo-tw.avif` 9rem, Claim, Lottie-Socials, Nav, Email-Block mit `icon-email.svg` + Gold-Link mailto?subject=Hey%20Tristan, Legal-Zeile „Impressum • Datenschutz", upgreight-Credit) · CookieConsent.astro + consent.ts (Banner unten 2rem-Margin #0C0C0C radius 1.5rem Grid 1.5fr/1fr, „Alle akzeptieren" Glow / „Ablehnen" tertiär / „Cookie Einstellungen bearbeiten"; Manager-Icon unten links Gold-Cookie; Prefs-Modal 4 Kategorien (Toggles: checked → Knopf x+20px, Fläche Gold #806429), localStorage, opt-in; Event/Callback für Vimeo-Gate; Texte in global-chrome.md §9) · CtaModal.astro (KOMPLETTE Spezifikation global-chrome.md §8: Felder, bedingte Radio-Logik Service-Typ/Unternehmens-Typ, choices.js 11.2.3 (Dep pinnen, lazy beim ersten Öffnen laden, Multiselect-Optionen aus services nach formName/Kategorie, Hidden-Input `Coaching-Bereiche`), eigene Fehlermeldungen (#E05C5C, Texte in Spec), ESC/Backdrop/X schließen, ?openModal=true) · AioModal.astro (all-in-one-coaching.md §19 — einfaches Formular, gleiche Validierungs-UX) — beide Modals global in BaseLayout, Formulare: action Form.Taxi-PLATZHALTER `https://form.taxi/s/FORM_TAXI_ENDPOINT_PLATZHALTER` (klar erkennbar!), fetch-Submit mit Accept:application/json, Erfolg inline („Vielen Dank! Ich melde mich in Kürze…"), Fehler inline; /danke-Seite als Redirect-Fallback (noindex).
6. **BaseLayout** anpassen: Poppins-Preloads (light+regular), favicon.png statt svg, theme-color #0C0C0C, color-scheme dark, Script-Einbindung: widgets/modals/consent immer, motion nur wenn [data-anim]/[data-hero-scroll]/… vorhanden. PageHeader.astro-Nutzung aus index.astro/[...slug].astro ENTFERNEN (Sections übernehmen alles). SEO.astro-Titlesuffix-Fix (siehe oben). JSON-LD: Person/Organization „Tristan Weithaler".
7. **Scripts:** `src/scripts/widgets.ts` (delegiert: FAQ (GSAP-Höhe, Plus→Minus), Tabs, Read-More 216 Zeichen + „weiterlesen", Testimonials-Load-More (nächste N=3? Original lädt seitenweise mit 250ms-Stagger), Vimeo-Player (Mute/Replay per Player-API nach Consent)) · `src/scripts/modals.ts` (open/close + Formularlogik) · `src/scripts/consent.ts` · `src/scripts/motion.ts` KOMPLETT NEU nach docs/webflow-spec/animations.md: lines (GSAP SplitText mask:lines, data-speed/-delay/-stagger), reveal-up (0.8s power3.out +0.3 delay, blur(5px)→0, y 1rem), reveal-left (FAQ x-100%), reveal-split (USP-Zeilen ∓1/1.5rem), reveal-x (Buttons x40px), fade, Scrubs: [data-hero-scroll] (Wipe opacity 1→0 + Bild-Reveal über 300vh), [data-results] (Karten auffächern rotate/x/y, Titel-Ebenen gegenläufig), [data-gallery] (x-Drift der 180%-Reihe, nur Desktop ≥992), [data-banner] (Testimonial-Zeilen gegenläufig x), [data-timeline] (Pin 1200vh, Liste x über 9 Stationen, ≤479 deaktiviert), [data-line-draw] (scaleX 0→1), [data-wipe] (About-Portrait), Glow-Follow (quickTo auf [data-glow-circle], Desktop), work-item-Hover (Bekannt-aus-Karten: Overlay-Slide+Zoom+Blur, Desktop), prefers-reduced-motion → alles aus. **WICHTIG: Initialzustände NUR per JS setzen (gsap.set), nie im CSS — sonst unsichtbarer Content ohne JS.** Für Scroll-Layouts (300vh/200vh/1200vh-Strecken): CSS-Basis ist bereits „animiert" gebaut → prüfen und ggf. auf `html.has-motion`-Gate umstellen (motion.ts setzt die Klasse), damit ohne JS/Reduced-Motion statisch gestapelt gerendert wird. Lottie: lottie-web (light) lazy für Footer-Icons.
8. **Seiten:** index.astro (getHome → SectionsHost, WebSite+Person JSON-LD), [...slug].astro (ohne PageHeader), 404.astro (gebrandet DE), danke.astro (noindex). Sitemap-Filter: /danke ausschließen (noindex-Hook fängt Legal ohnehin).
9. **QA:** astro check 0, studio tsc 0, sanity schema validate 0/0, `npm run build` (React-frei-Hook MUSS grün), preflight: `node .claude/skills/upgreight-astro-website/scripts/preflight.mjs ./dist --site https://tristanweithaler.com`, build:preview + `wrangler dev` Smoke (ohne Cookie statisch, mit Draft-Cookie Island), dist auf 0× data-sanity/Token greppen. Dependencies aufräumen: lenis raus, choices.js + lottie-web (light) rein — Versionen exakt pinnen.
10. **Provisioning** (NACH Julians CF-Fix; account_id pinnen!): `npm run provision -- --name tristanweithaler --prod-domain tristanweithaler.com --studio-host tristanweithaler` erst DRY-RUN prüfen, dann `--execute`. Danach .env/studio/.env prüfen, preview-Build gegen echtes Sanity, Studio-Feel. Manuelle Dashboard-Schritte (Workers Builds Git-Anbindung am Repo trischi1993/tw-website IN TRISTANS KONTO, Custom Domain tristanweithaler.com erst bei Go-Live/DNS-Umstellung durch Julian, Sanity-Publish-Webhook → Prod-Deploy-Hook) für Julian dokumentieren. GitHub push (Remote steht).
11. **README.md aufs Projekt anpassen** (Template-Beschreibung → Tristan-Projekt: Seiten, Sections, Seed/Sanity-Workflow, Deploy-Topologie mit Tristans Konto) + **Review-Agent** (separater Agent über finalen Stand), sinnvolle Funde fixen, dann Abschluss-Summary an Julian mit: Umfang, Sanity-Aufbau, Click-to-edit-Status, Animationen, GitHub/Cloudflare, URLs, Checks, Review-Funde, offene Platzhalter (Form.Taxi-Endpunkt! Vimeo-Poster, Datenschutztext veraltet (nennt Webflow etc.), DNS-Umstellung), Fokuspunkte fürs visuelle Review (Hero-Scroll, Results-Fächer, Timeline, Glow-Button, Modals, Cookie-Banner).

## Konventionen der neuen Komponenten (bereits umgesetzt)

- Interaktions-Hooks: `data-modal-open="cta|aio"`, `data-tabs`/`data-tab`/`data-tab-panel`, `data-faq-toggle`/`data-faq-panel`/`data-faq-item`, `data-read-more`, `data-load-more` (+`data-initial-count` am Grid), `data-vimeo`/`data-vimeo-consent`/`data-vimeo-accept`/`data-vimeo-controls`/`data-action="toggle-mute|replay"`, `data-glow-circle`.
- Animations-Hooks: `data-anim="lines|reveal-up|reveal-left|reveal-split|reveal-x|fade"` (+data-delay/-stagger/-speed), `data-hero-scroll`/`data-hero-wipe`/`data-hero-scroll-line`, `data-results`/`data-results-title-1/2`/`data-results-card`, `data-gallery`, `data-banner`/`data-banner-top`/`data-banner-bottom`, `data-timeline`/`data-timeline-track`/`data-timeline-list`, `data-line-draw`, `data-wipe`, `data-split-a`/`data-split-b`.
- Click-to-edit: jede Section stampft `edit?.(path)` auf Root + Textfelder (Muster siehe Komponenten). data-section-key nur wenn edit gesetzt.

## Sonstiges / Gotchas

- Sanity-CLI eingeloggt (julian@upgreight.com); Projekte Vita/Frinighof/template-live-test sichtbar; kein --org nötig (CLI-Default wie bei den Bestandsprojekten). Free-Plan, Backup via provision automatisch.
- .env (gitignored) steht auf Seed-Modus; studio/.env enthält nur SANITY_STUDIO_UI_LOCALE=de. provision füllt beide.
- StringRule.uri-Typcasts in studio (cta.ts/navItem.ts/richText.ts) — Laufzeit ok, nicht „zurückfixen".
- gh als „upgreight" eingeloggt, Repo trischi1993/tw-website leer, WRITE ok.
- Original-Doppelleerzeichen/Typos im Copy wurden beim Seed leicht normalisiert (Spec-Empfehlung).
- Das Testimonial „Friedrich Moosmair" hat order null → ans Ende (coalesce 9999 in der Query, im Seed ebenso ans Ende sortieren).
